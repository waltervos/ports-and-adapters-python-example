[project]
name = "ports-and-adapters-python-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3.4",
    "pytest-watcher>=0.4.3",
    "pendulum>=3.0.0",
]
